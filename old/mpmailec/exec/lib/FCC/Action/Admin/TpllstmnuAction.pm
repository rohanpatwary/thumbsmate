package FCC::Action::Admin::TpllstmnuAction;
$VERSION = 1.00;
use strict;
use warnings;
use base qw(FCC::Action::Admin::_SuperAction);
use CGI::Cookie;

sub dispatch {
	my($self) = @_;
	my $context = {};
	#
	return $context;
}

1;
